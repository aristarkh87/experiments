image: aristarkh87/ansible

stages:
  - check
  - build

before_script:
  - echo 'before'

check:
  stage: check
  script:
    - ansible-playbook --syntax-check playbook.yml
    - echo 'check code'

build:
  stage: build
  script:
    - echo 'build'
