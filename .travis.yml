language: python
python:
 - "2.7"
 - "3.6"

install:
  - pip install ansible==2.6.13 ansible-lint==3.5.0

script:
  - ansible-playbook -vvv --syntax-check playbook.yml
  - ansible-lint -v playbook.yml
